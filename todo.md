# حالة المهمة الحالية: تسجيل المتاجر وقاعدة البيانات

## الإصلاحات المنفذة

- [x] التحقق من أن نموذج تسجيل المتجر يصل إلى مرحلة إنشاء الحساب دون خطأ `restaurants.slug`.
- [x] إنشاء/إصلاح الجداول الأساسية المطلوبة لمسار التسجيل في قاعدة الموقع المنشورة.
- [x] اختبار التسجيل الفعلي على النسخة المنشورة وإنشاء مطعم تجريبي بنجاح.
- [x] ربط حساب مدير المتجر بالمطعم المنشأ تلقائيًا بدل ترك `restaurantId` فارغًا.
- [x] تعيين قالب منيو مساء `customer` للمتاجر الجديدة مع دعم العربية والإنجليزية والفرنسية.
- [x] إزالة default غير المدعوم من أعمدة `TEXT` في مخطط Drizzle وترحيلاته لتوافق TiDB/Hostinger.
- [x] تنظيف بيانات المطعم والحساب التجريبي بعد نجاح الاختبار.
- [x] تشغيل `pnpm check` و`pnpm build` بنجاح.
- [x] رفع الإصلاحات إلى فرع `main` في GitHub.
- [x] حفظ checkpoint WebDev بعد الإصلاح.

## ملاحظة النطاق

تمت أرشفة قائمة المتطلبات التاريخية غير المرتبطة بهذا الإصلاح خارج قائمة التنفيذ النشطة. لم تُحذف أي وظيفة من المشروع؛ التغيير الحالي يركز على التسجيل، قاعدة البيانات، وربط حساب مدير المتجر بالمنيو الموحد.
