Mobile visual verification at 390x844:
- Hamburger button is positioned at the far right of the top bar.
- A real rendered QR SVG is visible below the side logo and social icons.
- QR links to the menu section and is visually larger.
- TypeScript check and Vitest suite pass.

Browser interaction note:
- Initial browser tab had stale HMR state, so its interaction result reflected the previous menu implementation.
- Source and dev-server response confirm the new booking button, reservation modal, working hours, categories, and deployed QR URL are present.
- Removed an unintended trailing text fragment from client/index.html before final retest.
After reloading the preview, the stray trailing text is gone. The rendered QR SVG remains present and the menu anchor is clickable. The first synchronous interaction probe did not wait for React state updates, so a delayed asynchronous probe is required for the menu and reservation modal.
The delayed browser interaction test passed: the mobile menu opens, category controls and working hours are present, the reservation modal opens, five required fields are rendered, and the QR is a real SVG linked to the menu section. The encoded QR value points to the deployed menu URL.
Latest 390x844 preview confirms the mobile hero is substantially shorter, the menu heading and category tabs start immediately beneath it without the previous empty gap, and the first two real source-menu products render with their original images. The lower story image and its content were removed.
Final browser verification passed: 18 category tabs are rendered (All plus 17 source sections), all 135 product cards are present, the removed story section is absent, and the menu section is the immediate next section after the hero.
